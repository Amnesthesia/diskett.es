
<div class="col-lg-12" class="topbar" id="search-area"> 
      <!-- Right Nav Section --> 
        <form method="get" {{action update on='submit'}}>
          {{view Ember.TextField valueBinding="search_text" placeholder="Type to filter, hit enter to search" keyDown="filterGrid"}}
        </form>
</div>

<div class="container seethrough">
<div class="row">
  
    {{#each show in controller}}
    {{#if show.matchFilter}}
    <div class="item col-md-3 browse-list browse-item overlay-trigger" id="{{unbound show.id}}">
          <div class="progress browse-item-rating-progress">
            
            <div class="progress-bar {{unbound show.progressType}}" style="width: {{unbound this.ratingLength}}%">
              <div class="browse-item-rating">{{show.ratingText}}</div>
            </div>
        
          </div>
          <div class="browse-item-splitter">
            {{#link-to-animated "show" show animations="main:slideRight"}}
              <img src="../media/posters/{{unbound show.poster}}" alt="media/posters/{{unbound show.poster}}" />
            
            
              <h5>{{show.name}}</h5>
            {{/link-to-animated}}
          </div>
          {{#if show.isLoggedIn }}
          <div class="browse-item-actions">
             
            {{#if show.notFollowing}}
              <button class="btn btn-small btn-primary follow-button" {{action follow show.id}}> +Follow</button>
            {{else}}
              <button class="btn btn-small follow-button-grey"><i>Following</i></button>
            {{/if}} 
              
              {{#link-to-animated "show" show animations="main:slideRight"}}<button class="btn btn-small btn-info read-more-button"> More </button>{{/link-to-animated}}
              
              <div class="clear">&nbsp;</div>
          </div>
          {{/if}}
        </div>
    {{/if}}
    {{/each}}
</div>
<div class="row">
<button class="col-lg-12 btn btn-info" {{action forward}}>Load more</button>
</div>
</div>